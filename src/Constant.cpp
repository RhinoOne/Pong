#include "..\include\Constant.h"

namespace constants
{
    extern const int WINDOW_WIDTH(1280);
    extern const int WINDOW_HEIGHT(720);
    extern const float PADDLE_SPEED(0.8f);
    extern const float BALL_SPEED(0.9f);
}
